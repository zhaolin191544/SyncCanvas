## instructions

### bug report
- 线条 箭头复制bug。具体表现为：如果被复制的父元素设置了中间可以拉伸的点，那复制之后会共享这个点，导致移动复制后的子元素父元素也跟着改变。我需要子元素具有独立的扭曲拉伸点。
- 画笔元素在复制之后子元素和父元素重叠 我需要复制的元素有一定的偏移区分
- 图层中的UI更新过慢，比如说我删除某元素，他需要鼠标移出再移回等他重新渲染，设置可见性、编辑名称等操作同理，UI更新过慢
- 编辑名称操作我需要点击元素名称左侧的编辑logo才开启，现在当我鼠标移到元素名称区域时就全是编辑了，我可能本意只是想选择该元素